/** Number of questions per quiz session (single source of truth). */
export const QUIZ_SESSION_SIZE = 10;

/** Minimum confirmed words required to build a 4-option quiz. */
export const QUIZ_MIN_WORDS = 4;

/** Total answer options per question (1 correct + 3 distractors). */
export const QUIZ_OPTIONS_COUNT = 4;

/** Maximum finished-session results kept in quiz history. */
export const QUIZ_HISTORY_LIMIT = 50;
